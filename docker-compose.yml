services:
  vault:
    image: hashicorp/vault:latest
    container_name: vault-dev
    ports:
      - "8200:8200"
    cap_add:
      - IPC_LOCK
    environment:
      # UI を有効化（-ui でもOKだが、明示しておく）
      VAULT_UI: "true"
      VAULT_DEV_ROOT_TOKEN_ID: "root"
      VAULT_DEV_LISTEN_ADDRESS: "0.0.0.0:8200"
    command:
      - vault
      - server
      - -dev
      # - -dev-root-token-id=root
      # - -dev-listen-address=0.0.0.0:8200

  # 初期化: KV v2 を secret/ に有効化して、テストデータを書き込む
  vault-init:
    image: hashicorp/vault:latest
    depends_on:
      - vault
    environment:
      VAULT_ADDR: "http://vault:8200"
      VAULT_TOKEN: "root"
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        set -eu
        echo "[vault-init] waiting vault..."
        until vault status >/dev/null 2>&1; do
          sleep 0.2
        done

        vault secrets enable -path=secret kv-v2 >/dev/null 2>&1 || true
        vault kv put secret/app/prod username=alice password=pa55w0rd >/dev/null
        echo "[vault-init] done."
    restart: "no"

  # MinIO (S3 互換ストレージ)
  minio:
    image: minio/minio:latest
    container_name: minio-dev
    ports:
      - "9000:9000"  # API
      - "9001:9001"  # Web UI
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    command: server /data --console-address ":9001"
    volumes:
      - minio-data:/data
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 5s
      timeout: 5s
      retries: 5

  # MinIO 初期化: テスト用バケットとデータを作成
  minio-init:
    image: minio/mc:latest
    depends_on:
      minio:
        condition: service_healthy
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        set -eu
        echo "[minio-init] configuring MinIO client..."
        mc alias set minio http://minio:9000 minioadmin minioadmin

        echo "[minio-init] creating test bucket..."
        mc mb minio/kvtool-test --ignore-existing

        echo "[minio-init] uploading test data..."
        echo '{"app_name":"kvtool","version":"1.0.0"}' | mc pipe minio/kvtool-test/config/app.json
        echo 'APP_NAME=kvtool
        APP_ENV=production
        DATABASE_URL=postgres://localhost/mydb' | mc pipe minio/kvtool-test/config/production.env

        echo "[minio-init] done."
    restart: "no"

volumes:
  minio-data:
